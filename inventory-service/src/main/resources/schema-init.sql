-- ============================================================
-- Inventory Schema Initialization
-- Run this ONCE to create the 'inventory' schema in fnb_db
-- JPA (ddl-auto: update) will auto-create tables afterward
-- ============================================================
CREATE SCHEMA IF NOT EXISTS inventory;
